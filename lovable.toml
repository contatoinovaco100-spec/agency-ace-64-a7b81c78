[run]
build = "sh -c 'cd remix-of-agency-hub && npm install && npm run build && cp -r dist ../'"
install = "sh -c 'cd remix-of-agency-hub && npm install'"
command = "sh -c 'cd remix-of-agency-hub && npm run dev'"
